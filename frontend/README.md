# Frontend (React + Vite)

1. Instalar dependencias:

```bash
cd frontend
npm install
```

2. Ejecutar en desarrollo:

```bash
npm run dev
```

Asegúrate de tener el backend corriendo en `http://localhost:3000` o ajustar `VITE_API_URL`.
